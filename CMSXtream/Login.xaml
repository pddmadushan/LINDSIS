<Window x:Class="WpfApplication2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="Auto" Width="Auto" WindowStyle="None" AllowsTransparency="True" WindowState="Maximized" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" 
         Icon="Image/featherIcon.png" >
    <Window.Resources>
        <Style x:Key="MyWaterMarkStyle" TargetType="{x:Type TextBox}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Grid>
                            <Border Background="White" BorderBrush="#FF7D8683" BorderThickness="1"/>
                            <ScrollViewer x:Name="PART_ContentHost" Margin="5,0,0,0" VerticalAlignment="Center" />
                            <Label Margin="5,0,0,0" x:Name="WaterMarkLabel" Content="{TemplateBinding Tag}" VerticalAlignment="Center"
                               Visibility="Collapsed" Foreground="Gray" FontFamily="Arial"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Visibility" TargetName="WaterMarkLabel" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="DimGray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </Window.Resources>
    <Window.Background>
        <ImageBrush x:Name="imgBackground"  ImageSource="Image/imgFetherBacground.jpg" />
    </Window.Background>
    <Grid Name="Grid1">
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="50" />
            <ColumnDefinition Width="210" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="100" />
            <RowDefinition Height="300" />
            <RowDefinition Height="*"  />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Button Content="LOGIN"  Grid.Column="1" Grid.Row="3" HorizontalAlignment="Right"  VerticalAlignment="Top" Width="70" Click="Button_Click" Background="{x:Null}" FontWeight="Bold">
            <Button.Foreground>
                <SolidColorBrush Color="Black" Opacity="0.75"/>
            </Button.Foreground>
        </Button>
        <Button  Grid.Column="2" Content="" Width="20" HorizontalAlignment="Right" Click="Button_Click_1" ToolTip="Close" Margin="0,0,5,5" >
            <Button.Background>
                <ImageBrush ImageSource="Image/imgClose.png"/>
            </Button.Background>
        </Button>
        <Label x:Name="lblName" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="4" Content="LINDSIS ENGLISH INSTITUTE" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="25" FontWeight="Bold" Opacity="0.7" Margin="50,0,0,0" Foreground="White" Background="{x:Null}"   />
        <Label x:Name="lblslogan" Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="5" Content="The Simplest Way to the Language" Foreground="#FFCEDFF3" FontSize="16" Opacity="0.8" Margin="50,0,0,0" />
        <TextBox Name="txtUserName" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="23"  Grid.Row="1" TextWrapping="Wrap" VerticalAlignment="Top" Width="210"  Opacity="0.60" Margin="50,0,0,0" Style="{StaticResource MyWaterMarkStyle}" Tag="Username" />
        <PasswordBox Name="txtPassword" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Height="23"  Grid.Row="2" VerticalAlignment="Top" Width="210"  Opacity="0.60" Margin="50,0,0,0" Tag="Type Password" Password="^%!@#%$" GotFocus="PasswordBox_GotFocus" LostFocus="txtPassword_LostFocus"  />

        <Image Name="errorValidator" Source="Image/imgExclamation.png" Width="15" Height="15" Margin="5,0,0,0" Visibility="Hidden" HorizontalAlignment="Left"/>
    </Grid>


</Window>

<UserControl x:Class="CMSXtream.Control.SearchStudent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid Background="Aqua">
        <DataGrid x:Name="grdEmployee" HorizontalAlignment="Left"  VerticalAlignment="Top" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Header="Student ID" Binding="{Binding Path=STD_ID}"/>
                <DataGridTextColumn Header="Student Name" Binding="{Binding Path=STD_INITIALS}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

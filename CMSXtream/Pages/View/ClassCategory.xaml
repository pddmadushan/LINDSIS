<UserControl x:Class="CMSXtream.Pages.View.ClassCategory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:mui="http://firstfloorsoftware.com/ModernUI" 
             mc:Ignorable="d" >
    <UserControl.Resources >
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/DataGridStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/IconLibrary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </UserControl.Resources>
    <GroupBox x:Name="grpBoxCategory"  Grid.Row="2" Grid.Column="0" >
        <GroupBox.Header>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35" />
                    <ColumnDefinition Width="260" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition   />
                </Grid.RowDefinitions>
                <mui:ModernButton x:Name="btnAddNew" IconData="{Binding Data, Source={StaticResource ImgAddStudent}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Add Category" Click="btnAddNew_Click"  />
                <TextBox x:Name="groupTextSearch" TextChanged="groupTextSearch_TextChanged" Grid.Column="1" />
            </Grid>            
        </GroupBox.Header>
        <Grid Margin="0,5,0,0">
            <DataGrid Name="grdClsCategory" AutoGenerateColumns="False"  CanUserAddRows="False" RowHeaderWidth="0" EnableRowVirtualization = "True"  CanUserDeleteRows="False"  >
                <DataGrid.Columns  >
                    <DataGridTextColumn Header="ID" Binding="{Binding Path=CAT_ID}" IsReadOnly="True"  />
                    <DataGridTextColumn Header="Description" Binding="{Binding Path=CAT_NAME}" IsReadOnly="True" Width="250" />
                    <DataGridTextColumn Header="Sales Com Ptg" Binding="{Binding Path=CAT_COM_USER_PTG}" IsReadOnly="True" Width="100"/>
                    <DataGridTextColumn Header="Common Com Ptg" Binding="{Binding Path=CAT_COM_COMN_PTG}" IsReadOnly="True" Width="100" />
                    <DataGridTemplateColumn Width="50"  >
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <mui:ModernButton x:Name="btnEdit" IconData="{Binding Data, Source={StaticResource ImgEdit}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Edit Record" Click="btnEdit_Click"  />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </GroupBox>
</UserControl>

<UserControl x:Class="CMSXtream.Pages.View.StudentAttendanceHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <UserControl.Resources >
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/DataGridStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <DataGrid Name="grdAttHistory" AutoGenerateColumns="False"  CanUserAddRows="False" RowHeaderWidth="0" EnableRowVirtualization = "True"  CanUserDeleteRows="False"  >
            <DataGrid.Columns  >                
                <DataGridTextColumn Header="Week No" Binding="{Binding Path=CLS_WEEK}" IsReadOnly="True" Width="65" />
                <DataGridTemplateColumn Width="30" Header="Att" >
                    <DataGridTemplateColumn.CellTemplate   >
                        <DataTemplate >
                            <CheckBox Name="chkMark" IsChecked="{Binding Path=STD_ATT_FLG}" IsEnabled="{Binding Path=CLS_HOLD_FLG}" Checked="chkMark_Checked" Unchecked="chkMark_Unchecked" ></CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Class Name" Binding="{Binding Path=CLS_NAME}" IsReadOnly="True" Width="200" />
                <DataGridTextColumn Header="Class Date" Binding="{Binding Path=CLS_REC_DATE, StringFormat={}{0:dd/MM/yyyy}}" IsReadOnly="True" Width="100" />
                <DataGridCheckBoxColumn Header="Held" Binding="{Binding Path=CLS_HOLD_FLG}" IsReadOnly="True"  Width="50"/>               
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

<UserControl x:Class="CMSXtream.Pages.DataEntry.AddReson"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:mui="http://firstfloorsoftware.com/ModernUI"  >
    <UserControl.Resources >
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/DataGridStyle.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/StyleResource/IconLibrary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label  Grid.Row="0" Grid.Column="0" Content="Reason" />
        <Label Name="lblId" Content="-1" Visibility="Hidden"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="txtReason" TextAlignment="Left" TextChanged="txtReason_TextChanged"  />
        <Button x:Name="btnSave"  Grid.Row="0" Grid.Column="3"  ToolTip="Save Record" Padding="0,0" BorderThickness="1" Click="btnSave_Click" >
            <Image Name="imgSave" Source ="pack://application:,,,/Image/ImgBtnSave.png" Width="25" Height="31" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"   >
            </Image>
        </Button>
        <DataGrid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Name="grdReason" AutoGenerateColumns="False"  CanUserAddRows="False" RowHeaderWidth="0"  CanUserDeleteRows="False" PreviewMouseDoubleClick="grdReason_PreviewMouseDoubleClick" 
                  ToolTip="Double click on the row needed to be edited" Height="300">
            <DataGrid.Columns  >
                <DataGridTextColumn Header="No" Binding="{Binding Path=RSN_ID}" IsReadOnly="True" Width="70" Visibility="Hidden" />
                <DataGridTextColumn Header="Reason" Binding="{Binding Path=RSN_DES}" IsReadOnly="True" Width="300"/>
                <DataGridTemplateColumn Header="" Width="45" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <mui:ModernButton x:Name="btnDelete" IconData="{Binding Data, Source={StaticResource ImgDelete}}"  EllipseDiameter="25" EllipseStrokeThickness="1" ToolTip="Delete Reason" Click="btnDelete_Click" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

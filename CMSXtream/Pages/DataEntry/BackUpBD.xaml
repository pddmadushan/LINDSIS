<UserControl x:Class="CMSXtream.Pages.DataEntry.BackUpBD"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <Style
        TargetType="{x:Type Line}">
            <Setter Property="Stroke" Value="Black" />
            <Setter Property="StrokeThickness" Value="5.8" />
            <Setter Property="StrokeStartLineCap" Value="Round" />
            <Setter Property="StrokeEndLineCap" Value="Round" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button HorizontalAlignment="Left" Margin="73,73,0,0" VerticalAlignment="Top" Width="131" Click="Button_Click" ToolTip="Take a Database Back Up" Height="114">
            <Image Name="imgSave" Source ="pack://application:,,,/Image/ImgBackup.png" Width="109" Height="94" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            </Image>
        </Button>
        <Label Content="Backup Path : " HorizontalAlignment="Left" Margin="10,34,0,0" VerticalAlignment="Top"/>
        <Label x:Name="lblName" Content="" HorizontalAlignment="Left" Margin="89,34,0,0" VerticalAlignment="Top"/>
        <ProgressBar x:Name="pgrsBar" HorizontalAlignment="Left" Height="8" Margin="0,221,0,0" VerticalAlignment="Top" Width="297" Minimum="0" Maximum="100" RenderTransformOrigin="0.5,0.5" >
            <ProgressBar.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleY="-1"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </ProgressBar.RenderTransform>
        </ProgressBar>
    </Grid>
</UserControl>
